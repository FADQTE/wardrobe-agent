# store/cache 目录由 start.ps1 通过 npm_config_store_dir / npm_config_cache_dir
# 指向仓库内，不在此写死机器路径
registry=https://registry.npmmirror.com
# 跳过 pnpm run/exec 前的依赖自检（会触发 node_modules 清理）
verify-deps-before-run=false
